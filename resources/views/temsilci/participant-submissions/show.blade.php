<x-temsilci.app-layout :title="__('temsilci.participant_submissions.review')">
    <a href="{{ route('temsilci.participant-submissions.index') }}" class="ip-back-link">← {{ __('temsilci.participant_submissions.back') }}</a>
    <div class="ip-card"><div class="ip-section-title">{{ $approval->submission->entry->competition->name }}</div><div class="ip-section-hint">{{ $approval->submission->category->name }} · {{ $approval->submission->entry->user->first_name }} {{ $approval->submission->entry->user->last_name }}</div>
        <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:1rem;margin:1.25rem 0;">@foreach($approval->submission->photos as $photo)<figure style="margin:0;"><img src="{{ route('temsilci.participant-submissions.photos.show', $photo) }}" alt="" style="width:100%;aspect-ratio:4/3;object-fit:cover;border-radius:10px;"><figcaption style="margin-top:.5rem;">{{ $photo->captureDevice?->name ?? '—' }}</figcaption></figure>@endforeach</div>
        @if($approval->status->value === 'pending')<form method="POST" action="{{ route('temsilci.participant-submissions.decide', $approval) }}">@csrf<div class="ia-field"><label class="ia-label" for="note">{{ __('temsilci.participant_submissions.note') }}</label><textarea id="note" name="note" class="ia-input" rows="4">{{ old('note') }}</textarea></div><div style="display:flex;gap:.75rem;justify-content:flex-end;"><button class="ia-btn ia-btn-secondary" name="decision" value="reject">{{ __('temsilci.participant_submissions.reject') }}</button><button class="ia-btn" name="decision" value="approve">{{ __('temsilci.participant_submissions.approve') }}</button></div></form>@else<p>{{ __('temsilci.participant_submissions.already_decided') }}</p>@endif
    </div>
</x-temsilci.app-layout>
